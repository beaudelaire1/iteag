# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.local-*/

# Django
*.sqlite3
*.sqlite3-journal
*.sqlite3-shm
*.sqlite3-wal
db.sqlite3
/media/
staticfiles/

# Environment
.env
.env.local
.env.production

# Node
node_modules/
# package-lock.json est volontairement versionné : les builds frontend doivent
# installer exactement les dépendances validées par la CI et la production.

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log

# Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
.pytest_cache/

# Exports « dumpdata » de la base locale. Ils portent des données réelles —
# adresses des candidats, comptes — et changent à chaque export : leur place
# est sur le poste qui les produit, pas dans l'historique.
iteag_local*.json

# Tailwind output (regenerated)
static/css/main.css
# Bibliothèques tierces copiées depuis node_modules par « npm run vendor:build ».
static/js/vendor/
