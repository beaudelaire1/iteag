# Base dependencies — all environments
Django>=5.2,<5.3
wagtail>=7.4,<8.0
psycopg[binary]>=3.2,<4.0
redis>=5.0,<6.0
celery>=5.4,<6.0
django-redis>=5.4,<6.0
django-htmx>=1.21,<2.0
django-axes>=7.0,<8.0
django-jazzmin>=3.0,<4.0
django-csp>=4.0,<5.0
django-environ>=0.11,<1.0
django-otp>=1.5,<2.0
qrcode[pil]>=8.0,<9.0
# PYSEC-2026-3412 reste ouverte en amont, sans correctif publié : injection CSS
# lorsque les « presentational hints » HTML sont activés. La plateforme ne les
# active nulle part — les deux appels à write_pdf() gardent la valeur par
# défaut. Réévaluer si un appel venait à passer presentational_hints=True.
WeasyPrint>=68.0,<69.0
Pillow>=12.3,<13.0
