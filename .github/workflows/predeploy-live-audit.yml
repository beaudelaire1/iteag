name: Predeploy live audit

# Un contrôle contre une URL fixe n'a de valeur qu'après le déploiement du
# commit qu'il prétend auditer. Il s'exécute donc après un push sur main (et
# périodiquement), jamais sur une PR dont le code n'est pas encore servi par la
# préproduction. Le premier gate attend SOURCE_COMMIT puis refuse toute version
# qui ne correspond pas exactement à github.sha.
on:
  push:
    branches: [main]
  schedule:
    - cron: "20 3 * * 1"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: predeploy-live-${{ github.ref }}
  cancel-in-progress: true

jobs:
  preproduction:
    name: Preproduction HTTPS smoke
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      BASE_URL: https://iteag-preprod.137.74.169.188.sslip.io
    steps:
      - uses: actions/checkout@v6

      - name: Prouver la révision déployée et l'hôte SEO
        shell: bash
        run: |
          set -euo pipefail
          python src/scripts/verifier_preprod_deployee.py \
            --base-url "$BASE_URL" \
            --revision "$GITHUB_SHA" \
            --attente 600 \
            --intervalle 15

      - name: Vérifier les pages publiques critiques
        shell: bash
        run: |
          set -euo pipefail
          for path in \
            / \
            /presentation/ \
            /formations/ \
            /admissions/candidature/ \
            /connexion/ \
            /bibliotheque/ \
            /boutique/ \
            /robots.txt \
            /sitemap.xml \
            /healthz
          do
            code="$(curl --silent --show-error --max-time 20 --output /tmp/iteag-body --write-out '%{http_code}' "${BASE_URL}${path}")"
            printf '%-28s %s\n' "$path" "$code"
            test "$code" = "200"
          done

      - name: Vérifier la sonde applicative publique
        shell: bash
        run: |
          set -euo pipefail
          body="$(curl --silent --show-error --max-time 20 "${BASE_URL}/healthz")"
          echo "$body"
          python - "$body" <<'PY'
          import json
          import sys

          payload = json.loads(sys.argv[1])
          assert payload.get("statut") == "ok", payload
          # base/cache peuvent être volontairement masqués par HEALTHZ_JETON.
          # Leur contrôle profond appartient au gate serveur authentifié.
          PY

      - name: Vérifier les en-têtes de sécurité réels
        shell: bash
        run: |
          set -euo pipefail
          curl --silent --show-error --max-time 20 --dump-header /tmp/iteag-headers --output /dev/null "${BASE_URL}/"
          cat /tmp/iteag-headers
          for header in \
            '^strict-transport-security:' \
            '^content-security-policy:' \
            '^permissions-policy:' \
            '^x-content-type-options:' \
            '^x-frame-options:' \
            '^referrer-policy:' \
            '^x-robots-tag:' \
            '^x-iteag-revision:'
          do
            grep --ignore-case --quiet --extended-regexp "$header" /tmp/iteag-headers
          done
          grep --ignore-case --quiet --extended-regexp '^strict-transport-security:.*max-age=31536000' /tmp/iteag-headers
          grep --ignore-case --quiet --extended-regexp '^x-frame-options: DENY' /tmp/iteag-headers
          grep --ignore-case --quiet --extended-regexp '^content-security-policy:.*frame-ancestors '\''none'\''' /tmp/iteag-headers
          grep --ignore-case --quiet --extended-regexp '^permissions-policy:.*camera=\(\).*microphone=\(\).*geolocation=\(\).*usb=\(\)' /tmp/iteag-headers
          grep --ignore-case --quiet --extended-regexp '^x-robots-tag:.*noindex' /tmp/iteag-headers
          grep --ignore-case --quiet --fixed-strings "x-iteag-revision: ${GITHUB_SHA}" /tmp/iteag-headers

      - name: Auditer les liens internes publics
        shell: bash
        run: |
          set -euo pipefail
          python - "$BASE_URL" <<'PY'
          from html.parser import HTMLParser
          import sys
          from urllib.error import HTTPError, URLError
          from urllib.parse import urldefrag, urljoin, urlparse
          from urllib.request import Request, urlopen

          base = sys.argv[1].rstrip("/") + "/"
          origin = urlparse(base).netloc
          entrees = [
              base,
              urljoin(base, "presentation/"),
              urljoin(base, "formations/"),
              urljoin(base, "admissions/candidature/"),
              urljoin(base, "bibliotheque/"),
              urljoin(base, "boutique/"),
          ]

          class Liens(HTMLParser):
              def __init__(self):
                  super().__init__()
                  self.hrefs = []

              def handle_starttag(self, tag, attrs):
                  if tag != "a":
                      return
                  href = dict(attrs).get("href")
                  if href:
                      self.hrefs.append(href)

          def ouvrir(url):
              requete = Request(url, headers={"User-Agent": "ITEAG-Predeploy-Audit/1.0"})
              try:
                  with urlopen(requete, timeout=20) as reponse:
                      return reponse.status, reponse.geturl(), reponse.read()
              except HTTPError as exc:
                  return exc.code, exc.geturl(), exc.read()
              except URLError as exc:
                  raise SystemExit(f"Connexion impossible vers {url}: {exc}") from exc

          urls = set(entrees)
          for entree in entrees:
              code, _, corps = ouvrir(entree)
              if code != 200:
                  raise SystemExit(f"Page source {entree} -> {code}")
              parseur = Liens()
              parseur.feed(corps.decode("utf-8", errors="replace"))
              for href in parseur.hrefs:
                  if href.startswith(("mailto:", "tel:", "javascript:", "#")):
                      continue
                  url = urldefrag(urljoin(entree, href))[0]
                  analyse = urlparse(url)
                  if analyse.netloc != origin:
                      continue
                  if analyse.path.startswith(("/static/", "/media/")):
                      continue
                  urls.add(url)

          echecs = []
          for url in sorted(urls):
              code, finale, _ = ouvrir(url)
              print(f"{code:3} {url} -> {finale}" if finale != url else f"{code:3} {url}")
              if code == 404 or code >= 500:
                  echecs.append((url, code))

          if echecs:
              raise SystemExit(f"Liens publics cassés: {echecs}")
          print(f"{len(urls)} URL internes contrôlées sans 404/5xx.")
          PY

      - name: Vérifier sitemap et métadonnées essentielles
        shell: bash
        run: |
          set -euo pipefail
          python - "$BASE_URL" <<'PY'
          from html.parser import HTMLParser
          import sys
          from urllib.parse import urljoin, urlparse
          from urllib.request import Request, urlopen
          from xml.etree import ElementTree

          base = sys.argv[1].rstrip("/") + "/"
          hote_attendu = urlparse(base).netloc

          class Meta(HTMLParser):
              def __init__(self):
                  super().__init__()
                  self.title = ""
                  self.h1 = 0
                  self.canonical = ""
                  self.description = ""
                  self._title = False

              def handle_starttag(self, tag, attrs):
                  data = dict(attrs)
                  if tag == "h1":
                      self.h1 += 1
                  elif tag == "link" and data.get("rel") == "canonical":
                      self.canonical = data.get("href", "")
                  elif tag == "meta" and data.get("name") == "description":
                      self.description = data.get("content", "")
                  elif tag == "title":
                      self._title = True

              def handle_endtag(self, tag):
                  if tag == "title":
                      self._title = False

              def handle_data(self, data):
                  if self._title:
                      self.title += data

          def lire(url):
              req = Request(url, headers={"User-Agent": "ITEAG-Predeploy-Audit/1.0"})
              with urlopen(req, timeout=20) as rep:
                  return rep.status, rep.read()

          code, xml = lire(urljoin(base, "sitemap.xml"))
          assert code == 200
          racine = ElementTree.fromstring(xml)
          lieux = [elt.text.strip() for elt in racine.iter() if elt.tag.endswith("loc") and elt.text]
          assert lieux, "Sitemap vide"
          hotes = {urlparse(url).netloc for url in lieux}
          assert hotes == {hote_attendu}, f"Hôtes sitemap incohérents: {sorted(hotes)} != {hote_attendu}"
          print(f"Sitemap: {len(lieux)} URL déclarées sur {hote_attendu}")

          for path in ("", "presentation/", "formations/", "admissions/candidature/"):
              url = urljoin(base, path)
              code, html = lire(url)
              assert code == 200
              meta = Meta()
              meta.feed(html.decode("utf-8", errors="replace"))
              assert meta.title.strip(), f"title absent: {url}"
              assert meta.h1 == 1, f"nombre de H1={meta.h1}: {url}"
              assert meta.description.strip(), f"meta description absente: {url}"
              assert meta.canonical == url, f"canonical incohérente: {url}: {meta.canonical}"
              print(f"OK meta: {url} | H1={meta.h1} | canonical={meta.canonical}")
          PY
